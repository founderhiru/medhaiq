<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AI Interview Setup — MedhaIQ</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:wght@400;500;600&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/brand-logo.css">
  <style>
    :root {
      --bg:       #070b14;
      --surface:  #0d1424;
      --card:     #111a2e;
      --border:   rgba(255,255,255,0.07);
      --blue:     #1f59b4;
      --blue-hi:  #3b82f6;
      --blue-glow: rgba(59,130,246,0.15);
      --violet:   #8b5cf6;
      --muted:    #94a3b8;
      --dim:      #475569;
      --white:    #ffffff;
      --emerald:  #10b981;
      --gold:     #f59e0b;
      --orange:   #fb923c;
      --purple:   #a78bfa;
      --green:    #22c55e;
      --red:      #dc2626;
      /* Finalized 5-vector colors — same as live terminal & reports */
      --v-structure:  #3b82f6;
      --v-domain:     #a78bfa;
      --v-strategic:  #22c55e;
      --v-comm:       #fb923c;
      --v-leadership: #f59e0b;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body {
      background: var(--bg);
      color: var(--white);
      font-family: 'Inter', system-ui, sans-serif;
      font-size: 14px;
      line-height: 1.5;
      min-height: 100vh;
    }
    h1,h2,h3 { font-family: 'Plus Jakarta Sans', system-ui, sans-serif; font-weight: 700; }
    ul { list-style: none; }

    /* ── Nav ─────────────────────────────────────────── */
    .nav {
      position: sticky; top: 0; z-index: 20;
      background: rgba(7,11,20,0.95);
      backdrop-filter: blur(12px);
      border-bottom: 1px solid var(--border);
      height: 52px;
      overflow: hidden;
      display: flex; align-items: center;
      padding: 0 24px;
      justify-content: space-between;
    }
    .nav-actions { display: flex; align-items: center; gap: 10px; }
    .nav-link { color: var(--muted); text-decoration: none; font-size: 13px; font-weight: 500; padding: 5px 10px; border-radius: 6px; transition: color 0.15s; }
    .nav-link:hover { color: var(--white); }

    /* ── Master 3-column grid — zoom-stable clamped rails (v2.0).
       clamp() pins the side rails between hard floors and ceilings so
       browser zoom or window resize can never stretch or crush them;
       the wizard column absorbs the remaining space. min-width:0 on
       children stops any content from widening its frame. ── */
    .layout {
      display: grid;
      grid-template-columns: clamp(220px, 18vw, 300px) minmax(0,1fr) clamp(260px, 22vw, 340px);
      gap: 18px;
      max-width: 1560px;
      margin: 0 auto;
      padding: 18px 20px 24px;
      align-items: start;
    }
    .layout > * { min-width: 0; }

    /* ── Shared card/panel styles ────────────────────── */
    .panel {
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: 14px;
      padding: 16px;
    }
    .panel + .panel { margin-top: 14px; }
    .panel-label {
      font-size: 10px; font-weight: 700; letter-spacing: 1.4px;
      text-transform: uppercase; color: var(--blue-hi);
      margin-bottom: 12px;
    }

    /* ── LEFT: Session Context ───────────────────────── */
    .ctx-item { display: flex; align-items: flex-start; gap: 10px; padding: 8px 0; border-bottom: 1px solid var(--border); }
    .ctx-item:last-child { border-bottom: none; }
    .ctx-icon { width: 28px; height: 28px; border-radius: 8px; background: var(--blue-glow); color: var(--blue-hi); display: flex; align-items: center; justify-content: center; font-size: 13px; flex-shrink: 0; }
    .ctx-k { font-size: 11px; color: var(--dim); }
    .ctx-v { font-size: 13px; font-weight: 600; color: var(--white); }

    /* ── LEFT: Session Breakdown donut ───────────────── */
    .donut-wrap { display: flex; align-items: center; gap: 14px; }
    .donut {
      width: 92px; height: 92px; border-radius: 50%;
      background: conic-gradient(
        var(--v-structure) 0deg 126deg,
        var(--v-domain) 126deg 216deg,
        var(--v-strategic) 216deg 288deg,
        var(--v-comm) 288deg 324deg,
        var(--v-leadership) 324deg 360deg
      );
      display: flex; align-items: center; justify-content: center;
      flex-shrink: 0;
    }
    .donut-hole {
      width: 62px; height: 62px; border-radius: 50%;
      background: var(--surface);
      display: flex; flex-direction: column; align-items: center; justify-content: center;
    }
    .donut-num { font-family:'Plus Jakarta Sans',sans-serif; font-size: 20px; font-weight: 800; line-height: 1; }
    .donut-sub { font-size: 9px; color: var(--muted); }
    .legend { display: flex; flex-direction: column; gap: 5px; flex: 1; min-width: 0; }
    .legend li { display: flex; align-items: center; gap: 6px; font-size: 11px; color: var(--muted); }
    .legend .dot { width: 7px; height: 7px; border-radius: 50%; flex-shrink: 0; }
    .legend .pct { margin-left: auto; color: var(--white); font-weight: 600; }

    /* ── LEFT: Live Terminal Preview ─────────────────── */
    .prev-row { display: flex; align-items: center; gap: 8px; padding: 6px 0; font-size: 12px; color: var(--muted); }
    .prev-row .p-ic { width: 22px; height: 22px; border-radius: 6px; background: rgba(139,92,246,0.15); color: var(--violet); display: flex; align-items: center; justify-content: center; font-size: 11px; flex-shrink: 0; }
    .prev-ready { margin-left: auto; font-size: 10px; font-weight: 700; color: var(--emerald); background: rgba(16,185,129,0.12); padding: 2px 8px; border-radius: 100px; }
    .prev-msg { font-family: 'JetBrains Mono', monospace; font-size: 11px; color: var(--emerald); margin-top: 8px; }
    .live-pill { display: inline-flex; align-items: center; gap: 5px; font-size: 10px; font-weight: 700; color: var(--emerald); }
    .live-pill::before { content:''; width: 6px; height: 6px; border-radius: 50%; background: var(--emerald); animation: pulse 1.6s ease-in-out infinite; }
    @keyframes pulse { 0%,100%{opacity:1;transform:scale(1);} 50%{opacity:0.35;transform:scale(0.85);} }

    /* ── CENTER: wizard ──────────────────────────────── */
    .wizard-header { margin-bottom: 16px; }
    .wizard-header h1 { font-size: 22px; letter-spacing: -0.3px; display: flex; align-items: center; gap: 8px; }
    .wizard-header h1 .spark { color: var(--violet); }
    .wizard-header p { color: var(--muted); font-size: 13px; margin-top: 2px; }
    .step-label {
      font-family:'Plus Jakarta Sans',sans-serif;
      font-size: 14px; font-weight: 700; color: var(--white);
      margin: 20px 0 12px;
    }
    .step-label:first-of-type { margin-top: 0; }

    .role-tabs { display: flex; gap: 8px; margin-bottom: 14px; }
    .role-tab {
      padding: 7px 16px; border-radius: 8px;
      border: 1px solid var(--border); background: var(--card);
      color: var(--muted); font-size: 12px; font-weight: 600;
      cursor: pointer; font-family: inherit;
    }
    .role-tab.active { background: var(--blue); border-color: var(--blue); color: #fff; }
    .role-panel { display: none; }
    .role-panel.visible { display: block; }

    /* Role cards */
    .role-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; }
    .sel-card {
      position: relative;
      background: var(--card);
      border: 1px solid var(--border);
      border-radius: 12px;
      padding: 14px;
      cursor: pointer;
      transition: border-color 0.15s, transform 0.15s;
      user-select: none;
    }
    .sel-card:hover { border-color: var(--blue-hi); transform: translateY(-1px); }
    .sel-card.active {
      border-color: var(--blue-hi);
      box-shadow: 0 0 0 1px var(--blue-hi), 0 0 18px var(--blue-glow);
    }
    .sel-card .check {
      position: absolute; top: 10px; right: 10px;
      width: 20px; height: 20px; border-radius: 50%;
      background: var(--blue-hi); color: #fff;
      display: none; align-items: center; justify-content: center;
      font-size: 11px; font-weight: 700;
    }
    .sel-card.active .check { display: flex; }
    .rc-icon { width: 32px; height: 32px; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-size: 15px; margin-bottom: 10px; background: var(--blue-glow); }
    .rc-title { font-size: 13px; font-weight: 700; margin-bottom: 3px; }
    .rc-desc { font-size: 11px; color: var(--muted); line-height: 1.45; }
    .custom-input {
      display: none; width: 100%; margin-top: 10px;
      background: var(--card); border: 1px solid var(--blue-hi);
      color: var(--white); border-radius: 8px; padding: 10px 14px;
      font-family: inherit; font-size: 13px; outline: none;
    }
    .custom-input.show { display: block; }

    /* Experience cards */
    .exp-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; }
    .exp-card { text-align: left; display: flex; align-items: center; gap: 10px; }
    .exp-emoji { font-size: 18px; }
    .exp-name { font-size: 13px; font-weight: 700; }
    .exp-yrs { font-size: 11px; color: var(--muted); }

    /* Persona cards */
    .persona-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; }
    .persona-card { padding: 14px; }
    .p-head { display: flex; align-items: center; gap: 10px; margin-bottom: 8px; }
    .p-avatar {
      width: 38px; height: 38px; border-radius: 50%;
      background: linear-gradient(135deg, var(--blue), var(--blue-hi));
      display: flex; align-items: center; justify-content: center;
      font-family:'Plus Jakarta Sans',sans-serif; font-size: 13px; font-weight: 700;
      flex-shrink: 0;
    }
    .p-avatar.emerald { background: linear-gradient(135deg, #047857, #10b981); }
    .p-avatar.gold    { background: linear-gradient(135deg, #b45309, #f59e0b); }
    .p-name { font-size: 13px; font-weight: 700; }
    .p-title { font-size: 11px; color: var(--muted); }
    .difficulty { font-size: 10px; font-weight: 700; padding: 2px 8px; border-radius: 100px; white-space: nowrap; }
    .diff-hard   { background: rgba(220,38,38,0.15); color: #f87171; }
    .diff-elite  { background: rgba(139,92,246,0.15); color: #c4b5fd; }
    .diff-medium { background: rgba(245,158,11,0.15); color: #fcd34d; }
    .p-style { display: inline-block; font-size: 10px; font-weight: 700; padding: 3px 9px; border-radius: 6px; margin: 6px 0 8px; }
    .p-style.royal-blue { background: rgba(30,64,175,0.3); color: #93c5fd; }
    .p-style.emerald    { background: rgba(5,150,105,0.3);  color: #6ee7b7; }
    .p-style.gold       { background: rgba(217,119,6,0.3);  color: #fcd34d; }
    .p-traits { display: flex; flex-wrap: wrap; gap: 4px 10px; }
    .p-traits li { font-size: 10.5px; color: var(--muted); position: relative; padding-left: 9px; }
    .p-traits li::before { content:'•'; position: absolute; left: 0; color: var(--dim); }

    /* ── RIGHT: Live Terminal ────────────────────────── */
    .terminal { position: sticky; top: 66px; }
    .term-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 14px; }
    .term-title { font-family:'JetBrains Mono',monospace; font-size: 11px; font-weight: 700; letter-spacing: 0.8px; text-transform: uppercase; color: var(--muted); }
    .term-block { padding: 12px 0; border-bottom: 1px solid var(--border); }
    .term-block:first-of-type { padding-top: 0; }
    .term-block-last { border-bottom: none; }
    .term-label { font-size: 10px; font-weight: 700; letter-spacing: 1px; text-transform: uppercase; color: var(--dim); margin-bottom: 10px; }
    .engine-row { display: flex; align-items: center; gap: 12px; }
    .engine-ring-svg { flex-shrink: 0; filter: drop-shadow(0 0 8px rgba(139,92,246,0.45)); }
    .engine-title { font-family:'Plus Jakarta Sans',sans-serif; font-size: 14px; font-weight: 700; }
    .engine-sub { font-size: 11px; color: var(--muted); line-height: 1.5; }

    .star-row { display: flex; align-items: center; }
    .star-step { display: flex; flex-direction: column; align-items: center; gap: 5px; }
    .star-dot { width: 28px; height: 28px; border-radius: 50%; border: 1.5px solid var(--border); display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: 700; color: var(--muted); font-family:'Plus Jakarta Sans',sans-serif; }
    .star-step.on .star-dot { border-color: var(--blue-hi); color: var(--blue-hi); background: var(--blue-glow); }
    .star-name { font-size: 9px; color: var(--muted); }
    .star-step.on .star-name { color: var(--white); font-weight: 600; }
    .star-line { flex: 1; height: 1px; background: var(--border); margin: 0 3px 14px; }

    .score-row { display: flex; align-items: center; gap: 12px; }
    .score-ring { width: 52px; height: 52px; border-radius: 50%; border: 3px solid var(--border); display: flex; align-items: center; justify-content: center; font-family:'Plus Jakarta Sans',sans-serif; font-weight: 700; font-size: 13px; color: var(--dim); flex-shrink: 0; }
    .score-msg { font-size: 12px; font-weight: 600; color: var(--emerald); }
    .score-sub { font-size: 11px; color: var(--muted); }

    .vec-list { display: flex; flex-direction: column; gap: 10px; }
    .vec-list li { display: flex; align-items: center; gap: 8px; font-size: 12px; color: var(--muted); }
    .vec-ic { width: 20px; height: 20px; border-radius: 6px; display: flex; align-items: center; justify-content: center; font-size: 10px; flex-shrink: 0; }
    .vec-bar { flex: 1; height: 4px; border-radius: 2px; background: var(--border); }
    .vec-val { font-family:'JetBrains Mono',monospace; font-size: 11px; color: var(--dim); }

    .feed-row { display: flex; align-items: flex-start; gap: 7px; font-size: 12px; color: var(--muted); }
    .feed-dot { width: 6px; height: 6px; border-radius: 50%; background: var(--blue-hi); margin-top: 6px; flex-shrink: 0; }

    .begin-btn {
      width: 100%; margin-top: 14px;
      background: linear-gradient(135deg, var(--blue-hi), var(--violet));
      color: #fff; border: none; border-radius: 12px;
      padding: 14px; font-family:'Plus Jakarta Sans',sans-serif;
      font-size: 15px; font-weight: 700; cursor: pointer;
      transition: opacity 0.15s, transform 0.15s;
    }
    .begin-btn:hover:not(:disabled) { transform: translateY(-1px); }
    .begin-btn:disabled { opacity: 0.4; cursor: not-allowed; }
    .begin-sub { text-align: center; font-size: 10px; color: var(--muted); margin-top: 6px; }

    /* ── Responsive — v2.0: the old 1200px breakpoint was the D.png bug.
       A laptop zoomed to ~125–150% drops its effective viewport below
       1200px and the whole wizard collapsed to one giant column. The
       clamped rails above absorb the squeeze instead; only genuinely
       small screens (phones/small tablets) stack now. ── */
    @media (max-width: 820px) {
      .layout { grid-template-columns: 1fr; }
      .terminal { position: static; }
      .role-grid { grid-template-columns: repeat(2, 1fr); }
      .exp-grid { grid-template-columns: repeat(2, 1fr); }
      .persona-grid { grid-template-columns: repeat(2, 1fr); }
    }
    @media (max-width: 640px) {
      .role-grid, .exp-grid, .persona-grid { grid-template-columns: 1fr; }
    }
    /* ══════════════════════════════════════════════════════════════
       TAILOR-FOR-A-SPECIFIC-JOB — additive styles only. Uses existing
       tokens (--card, --border, --blue-hi, --green, --violet). Safe to
       remove wholesale without affecting any other rule.
    ══════════════════════════════════════════════════════════════ */
    .tailor-card{
      position:relative; background:var(--card);
      border:1px dashed rgba(139,92,246,.55); border-radius:14px;
      padding:14px; cursor:pointer; display:flex; flex-direction:column; gap:8px;
      transition:border-color .15s, box-shadow .15s;
    }
    .tailor-card:hover{ border-color:var(--violet); box-shadow:0 0 18px rgba(139,92,246,.25); }
    .tailor-card .tc-icon{ width:34px;height:34px;border-radius:10px;display:flex;align-items:center;justify-content:center;
      background:rgba(139,92,246,.12); color:var(--violet); }
    .tailor-card .tc-icon svg{ width:18px;height:18px;stroke:currentColor;fill:none;stroke-width:2;stroke-linecap:round; }
    .tailor-card .tc-title{ font-size:13px;font-weight:700; }
    .tailor-card .tc-desc{ font-size:11px;color:var(--muted);line-height:1.5; }
    .tc-add-btn{
      margin-top:auto; align-self:flex-start; display:inline-flex; align-items:center; gap:6px;
      background:transparent; border:1px solid rgba(139,92,246,.5); color:#c4b5fd;
      font:inherit; font-size:12px; font-weight:600; padding:6px 12px; border-radius:9px; cursor:pointer; transition:.15s;
    }
    .tc-add-btn:hover{ background:rgba(139,92,246,.12); }

    /* ── Success state ── */
    .tailor-card.tc-success{
      border:1px solid rgba(34,197,94,.55); border-style:solid;
      box-shadow:0 0 18px rgba(34,197,94,.22);
    }
    .tc-company-row{ display:flex; align-items:center; gap:8px; }
    .tc-check{
      width:20px;height:20px;border-radius:50%;flex-shrink:0;
      display:flex;align-items:center;justify-content:center;
      background:rgba(34,197,94,.15); color:var(--green); font-size:12px; font-weight:800;
    }
    .tc-company{ font-size:13px;font-weight:700; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
    .tc-role-line{ font-size:11.5px; color:var(--muted); white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
    .tc-badges{ display:flex; align-items:center; gap:6px; flex-wrap:wrap; }
    .tc-badge{
      font-size:10px; font-weight:600; padding:3px 8px; border-radius:6px;
      border:1px solid rgba(59,130,246,.45); color:#93c5fd; background:rgba(59,130,246,.08);
    }
    .tc-badge.ok{ border-color:rgba(34,197,94,.45); color:#86efac; background:rgba(34,197,94,.08); }
    .tc-metrics{ font-size:11px; color:var(--green); font-weight:600; }
    .tc-edit-link{
      margin-top:auto; align-self:flex-start;
      background:none; border:none; padding:0; cursor:pointer;
      font:inherit; font-size:11.5px; font-weight:600; color:var(--muted); text-decoration:underline; text-underline-offset:3px;
    }
    .tc-edit-link:hover{ color:#e2e8f0; }

    /* ── Modal ── */
    .tc-overlay{
      position:fixed; inset:0; z-index:200; display:none;
      align-items:center; justify-content:center; padding:20px;
      background:rgba(3,6,14,.72); backdrop-filter:blur(4px);
    }
    .tc-overlay.open{ display:flex; }
    .tc-modal{
      width:min(440px,100%); max-height:90vh; overflow-y:auto;
      background:var(--surface); border:1px solid var(--border); border-radius:18px;
      padding:20px; box-shadow:0 24px 60px rgba(0,0,0,.6);
    }
    .tc-m-head{ display:flex; align-items:center; justify-content:space-between; margin-bottom:14px; }
    .tc-m-title{ font-size:16px; font-weight:800; }
    .tc-m-close{ background:none;border:none;color:var(--muted);font-size:16px;cursor:pointer; }
    .tc-m-close:hover{ color:#fff; }
    .tc-field-label{ font-size:11px; font-weight:700; letter-spacing:.06em; text-transform:uppercase; color:var(--muted); margin:12px 0 6px; }
    .tc-field-label .opt{ font-weight:400; text-transform:none; letter-spacing:0; }
    .tc-input, .tc-textarea{
      width:100%; background:var(--card); border:1px solid var(--border); border-radius:10px;
      color:#e2e8f0; font:inherit; font-size:13px; padding:10px 12px; outline:none; transition:border .15s;
    }
    .tc-input:focus, .tc-textarea:focus{ border-color:var(--blue-hi); }
    .tc-textarea{ min-height:130px; resize:vertical; line-height:1.55; }
    .tc-count{ text-align:right; font-size:11px; color:var(--muted); margin-top:4px; }
    .tc-levels{ display:flex; flex-wrap:wrap; gap:6px; }
    .tc-level{
      background:var(--card); border:1px solid var(--border); color:var(--muted);
      font:inherit; font-size:11.5px; font-weight:600; padding:5px 11px; border-radius:8px; cursor:pointer; transition:.15s;
    }
    .tc-level.active{ border-color:var(--blue-hi); color:#bfdbfe; background:rgba(59,130,246,.12); }
    .tc-chips{ display:flex; flex-wrap:wrap; gap:6px; min-height:24px; }
    .tc-chip{
      font-size:10.5px; font-weight:600; padding:3px 9px; border-radius:7px;
      border:1px solid rgba(34,197,94,.4); color:#86efac; background:rgba(34,197,94,.07);
    }
    .tc-apply{
      width:100%; margin-top:16px; border:none; cursor:pointer;
      background:linear-gradient(90deg,#3b82f6,#8b5cf6); color:#fff;
      font:inherit; font-size:14px; font-weight:700; padding:12px; border-radius:12px;
      box-shadow:0 8px 24px rgba(99,102,241,.35); transition:filter .15s;
    }
    .tc-apply:hover{ filter:brightness(1.1); }
    .tc-privacy{ margin-top:10px; text-align:center; font-size:11px; color:var(--muted); }
    /* Company logo avatar (success state) — img with SVG building fallback */
    .tc-logo{
      width:24px;height:24px;border-radius:7px;flex-shrink:0;overflow:hidden;
      display:flex;align-items:center;justify-content:center;
      background:rgba(255,255,255,.06); border:1px solid var(--border); color:var(--muted);
    }
    .tc-logo-img{ width:18px;height:18px;object-fit:contain;border-radius:4px; }
    .tc-logo-fallback{ width:14px;height:14px; }
  </style>
</head>
<body>

<% if (typeof previewMode !== 'undefined' && previewMode) { %>
<script>window.__MIQ_PREVIEW_DEMO__ = <%- JSON.stringify(demoData || {}) %>;</script>
<% } %>

<nav class="nav">
<%- include('partials/brand-logo', { size: 'md', eager: true }) %>
  <div class="nav-actions">
    <a href="<%= typeof previewMode !== 'undefined' && previewMode ? '/auth/signup' : '/dashboard/history' %>" class="nav-link"><%= typeof previewMode !== 'undefined' && previewMode ? 'Sign Up' : 'History' %></a>
  </div>
</nav>

<div class="layout">

  <!-- ══════════ LEFT SIDEBAR ══════════ -->
  <aside>
    <div class="panel">
      <div class="panel-label">Session Context</div>
      <div class="ctx-item"><div class="ctx-icon">💼</div><div><div class="ctx-k">Role</div><div class="ctx-v" id="ctxRole">Not selected</div></div></div>
      <div class="ctx-item"><div class="ctx-icon">⚡</div><div><div class="ctx-k">Experience</div><div class="ctx-v" id="ctxExp">Not selected</div></div></div>
      <div class="ctx-item"><div class="ctx-icon">👤</div><div><div class="ctx-k">Interviewer</div><div class="ctx-v" id="ctxPersona">Not selected</div></div></div>
      <div class="ctx-item"><div class="ctx-icon">🕐</div><div><div class="ctx-k">Duration</div><div class="ctx-v">~45 minutes</div></div></div>
      <div class="ctx-item"><div class="ctx-icon">✦</div><div><div class="ctx-k">Difficulty</div><div class="ctx-v" id="ctxDiff">—</div></div></div>
      <div class="ctx-item" id="ctxCompanyWrap" style="display:none">
        <div class="ctx-icon"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M4 21V5a1 1 0 0 1 1-1h8a1 1 0 0 1 1 1v16"/><path d="M14 9h5a1 1 0 0 1 1 1v11"/><path d="M2 21h20"/><path d="M7.5 8h3M7.5 12h3M7.5 16h3M17 13h1M17 17h1"/></svg></div>
        <div>
          <div class="ctx-k">Target Company</div>
          <div class="ctx-v" id="ctxCompany">—</div>
          <div style="font-size:10.5px;color:var(--green);margin-top:2px" id="ctxCompanyMeta"></div>
        </div>
      </div>
    </div>

    <div class="panel">
      <div class="panel-label">Session Breakdown</div>
      <div class="donut-wrap">
        <div class="donut"><div class="donut-hole"><div class="donut-num">5</div><div class="donut-sub">Vectors</div></div></div>
        <ul class="legend">
          <li><span class="dot" style="background:var(--v-structure)"></span>Structure<span class="pct">35%</span></li>
          <li><span class="dot" style="background:var(--v-domain)"></span>Domain Expertise<span class="pct">25%</span></li>
          <li><span class="dot" style="background:var(--v-strategic)"></span>Strategic Thinking<span class="pct">20%</span></li>
          <li><span class="dot" style="background:var(--v-comm)"></span>Communication<span class="pct">10%</span></li>
          <li><span class="dot" style="background:var(--v-leadership)"></span>Leadership &amp; Exec.<span class="pct">10%</span></li>
        </ul>
      </div>
    </div>

    <div class="panel">
      <div class="panel-label" style="display:flex;justify-content:space-between;align-items:center;">Live Terminal Preview <span class="live-pill">Live</span></div>
      <div class="prev-row"><span class="p-ic">◎</span>STAR Engine<span class="prev-ready">Ready</span></div>
      <div class="prev-row"><span class="p-ic">◎</span>Behavior Analyzer<span class="prev-ready">Ready</span></div>
      <div class="prev-row"><span class="p-ic">◎</span>Sentiment Engine<span class="prev-ready">Ready</span></div>
      <div class="prev-row"><span class="p-ic">◎</span>Response Predictor<span class="prev-ready">Ready</span></div>
      <div class="prev-msg">All systems ready. Let's begin!</div>
    </div>
  </aside>

  <!-- ══════════ CENTER: WIZARD ══════════ -->
  <main class="panel" style="padding:20px;">
    <div class="wizard-header">
      <h1><span class="spark">✦</span> Build Your <span>Interview Experience</span></h1>
      <p>Choose your target role, experience level, and interview persona that best matches your target opportunity</p>
    </div>

    <div class="step-label">1. Select Target Role</div>
    <div class="role-tabs">
      <button class="role-tab active" data-tab="core" onclick="switchTab('core')">Core Roles</button>
      <button class="role-tab" data-tab="ai" onclick="switchTab('ai')">AI &amp; Data Roles</button>
    </div>

    <div class="role-panel visible" id="tab-core">
      <div class="role-grid">
        <div class="sel-card chip" data-role="Product Manager"><div class="check">✓</div><div class="rc-icon">💼</div><div class="rc-title">Product Manager</div><div class="rc-desc">Drive product strategy and execution</div></div>
        <div class="sel-card chip" data-role="Software Engineer"><div class="check">✓</div><div class="rc-icon">&lt;/&gt;</div><div class="rc-title">Software Engineer</div><div class="rc-desc">Design, build and scale software systems</div></div>
        <div class="sel-card chip" data-role="Engineering Manager"><div class="check">✓</div><div class="rc-icon">👥</div><div class="rc-title">Engineering Manager</div><div class="rc-desc">Lead engineering teams and deliver impact</div></div>
        <div class="sel-card chip" data-role="Solutions Architect"><div class="check">✓</div><div class="rc-icon">🗂</div><div class="rc-title">Solutions Architect</div><div class="rc-desc">Design scalable architectural solutions</div></div>
        <div class="sel-card chip" data-role="Technical Program Manager"><div class="check">✓</div><div class="rc-icon">🗓</div><div class="rc-title">Technical Program Manager</div><div class="rc-desc">Plan and deliver complex technical programs</div></div>
        <div class="sel-card chip" data-role="Management Consultant"><div class="check">✓</div><div class="rc-icon">📊</div><div class="rc-title">Management Consultant</div><div class="rc-desc">Solve business problems and drive transformation</div></div>
        <div class="sel-card chip" data-role="Executive Leadership"><div class="check">✓</div><div class="rc-icon">👑</div><div class="rc-title">Executive Leadership</div><div class="rc-desc">Set vision and lead at the executive level</div></div>

        <!-- ══ TAILOR FOR A SPECIFIC JOB — replaces the Custom Role tile in
             this grid. NOT a .sel-card: the role-selection handler must never
             treat it as a role. Instanced by CLASS (one per tab); a single
             shared modal + state drives every instance in sync. ══ -->
        <div class="tailor-card" role="button" tabindex="0" aria-haspopup="dialog">
          <!-- State A: unselected placeholder -->
          <div class="tc-state-empty">
            <div class="tc-icon">
              <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><circle cx="12" cy="12" r="4.5"/><circle cx="12" cy="12" r="1" fill="currentColor" stroke="none"/></svg>
            </div>
            <div class="tc-title">Tailor for a specific Job</div>
            <div class="tc-desc">Personalize this interview for a specific role, company, or job opportunity.</div>
            <button class="tc-add-btn" type="button">＋ Add Context</button>
          </div>
          <!-- State B: post-save success (hidden until Apply Context) -->
          <div class="tc-state-done" style="display:none">
            <div class="tc-company-row">
              <span class="tc-check">✓</span>
              <span class="tc-logo">
                <img class="tc-logo-img" alt="" width="18" height="18" style="display:none">
                <svg class="tc-logo-fallback" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M4 21V5a1 1 0 0 1 1-1h8a1 1 0 0 1 1 1v16"/><path d="M14 9h5a1 1 0 0 1 1 1v11"/><path d="M2 21h20"/><path d="M7.5 8h3M7.5 12h3M7.5 16h3"/></svg>
              </span>
              <span class="tc-company tc-company-out">—</span>
            </div>
            <div class="tc-role-line tc-role-out"></div>
            <div class="tc-badges">
              <span class="tc-badge ok tc-jd-badge" style="display:none">JD Attached</span>
              <span class="tc-badge tc-level-badge" style="display:none"></span>
            </div>
            <div class="tc-metrics tc-metrics-out"></div>
            <button class="tc-edit-link" type="button">Edit Context…</button>
          </div>
        </div>
      </div>
    </div>

    <div class="role-panel" id="tab-ai">
      <div class="role-grid">
        <div class="sel-card chip" data-role="AI Engineer"><div class="check">✓</div><div class="rc-icon">🤖</div><div class="rc-title">AI Engineer</div><div class="rc-desc">Build and deploy AI-powered systems</div></div>
        <div class="sel-card chip" data-role="AI Product Manager"><div class="check">✓</div><div class="rc-icon">🧭</div><div class="rc-title">AI Product Manager</div><div class="rc-desc">Ship AI products from idea to scale</div></div>
        <div class="sel-card chip" data-role="Data Engineer"><div class="check">✓</div><div class="rc-icon">🛠</div><div class="rc-title">Data Engineer</div><div class="rc-desc">Build reliable data pipelines and platforms</div></div>
        <div class="sel-card chip" data-role="ML Engineer"><div class="check">✓</div><div class="rc-icon">📈</div><div class="rc-title">ML Engineer</div><div class="rc-desc">Train, evaluate and productionize models</div></div>
        <div class="sel-card chip" data-role="MLOps Engineer"><div class="check">✓</div><div class="rc-icon">🔁</div><div class="rc-title">MLOps Engineer</div><div class="rc-desc">Operate ML systems reliably at scale</div></div>
        <div class="sel-card chip" data-role="Research Scientist"><div class="check">✓</div><div class="rc-icon">🔬</div><div class="rc-title">Research Scientist</div><div class="rc-desc">Advance the state of the art with research</div></div>
        <div class="sel-card chip" data-role="LLM Engineer"><div class="check">✓</div><div class="rc-icon">💬</div><div class="rc-title">LLM Engineer</div><div class="rc-desc">Build applications on large language models</div></div>

        <!-- ══ TAILOR FOR A SPECIFIC JOB — second instance (AI & Data tab).
             Identical markup to the Core tab card; the shared handler keeps
             both instances' states in sync automatically. ══ -->
        <div class="tailor-card" role="button" tabindex="0" aria-haspopup="dialog">
          <div class="tc-state-empty">
            <div class="tc-icon">
              <svg viewBox="0 0 24 24"><circle cx="12" cy="12" r="9"/><circle cx="12" cy="12" r="4.5"/><circle cx="12" cy="12" r="1" fill="currentColor" stroke="none"/></svg>
            </div>
            <div class="tc-title">Tailor for a specific Job</div>
            <div class="tc-desc">Personalize this interview for a specific role, company, or job opportunity.</div>
            <button class="tc-add-btn" type="button">＋ Add Context</button>
          </div>
          <div class="tc-state-done" style="display:none">
            <div class="tc-company-row">
              <span class="tc-check">✓</span>
              <span class="tc-logo">
                <img class="tc-logo-img" alt="" width="18" height="18" style="display:none">
                <svg class="tc-logo-fallback" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><path d="M4 21V5a1 1 0 0 1 1-1h8a1 1 0 0 1 1 1v16"/><path d="M14 9h5a1 1 0 0 1 1 1v11"/><path d="M2 21h20"/><path d="M7.5 8h3M7.5 12h3M7.5 16h3"/></svg>
              </span>
              <span class="tc-company tc-company-out">—</span>
            </div>
            <div class="tc-role-line tc-role-out"></div>
            <div class="tc-badges">
              <span class="tc-badge ok tc-jd-badge" style="display:none">JD Attached</span>
              <span class="tc-badge tc-level-badge" style="display:none"></span>
            </div>
            <div class="tc-metrics tc-metrics-out"></div>
            <button class="tc-edit-link" type="button">Edit Context…</button>
          </div>
        </div>
      </div>
    </div>
    <input type="text" class="custom-input" id="roleCustom" placeholder="Type your role and press Enter...">

    <div class="step-label">2. Select Career Stage</div>
    <div class="exp-grid" id="chips-exp">
      <div class="sel-card chip exp-card" data-exp="fresher"><div class="check">✓</div><span class="exp-emoji">🌱</span><div><div class="exp-name">Fresher</div><div class="exp-yrs">0–2 yrs</div></div></div>
      <div class="sel-card chip exp-card" data-exp="mid"><div class="check">✓</div><span class="exp-emoji">⚡</span><div><div class="exp-name">Mid-Career</div><div class="exp-yrs">3–8 yrs</div></div></div>
      <div class="sel-card chip exp-card" data-exp="senior"><div class="check">✓</div><span class="exp-emoji">🔥</span><div><div class="exp-name">Senior</div><div class="exp-yrs">8–15 yrs</div></div></div>
      <div class="sel-card chip exp-card" data-exp="executive"><div class="check">✓</div><span class="exp-emoji">👑</span><div><div class="exp-name">Executive</div><div class="exp-yrs">15+ yrs</div></div></div>
    </div>

    <div class="step-label">3. Select Interviewer Persona</div>
    <%
    // Persona entitlement enforcement (traced gap, closed here + in
    // controllers/sessionController.js). capabilities.personas is the
    // Capability Engine's resolved, entitled persona-ID list for this
    // user's active package (config/persona-entitlements.js). Locked
    // personas are not rendered at all below — never a CSS-hidden card
    // still present in the DOM — per the explicit "do not rely on CSS
    // hiding" requirement.
    //
    // Preview mode is the one deliberate exception: it's unauthenticated
    // marketing content (clicking "begin" always redirects to signup
    // regardless of persona chosen, see IS_PREVIEW below), not a real
    // entitlement-gated flow, so it continues to show all personas.
    const _isPreviewPersonas = typeof previewMode !== 'undefined' && previewMode;
    const _entitledPersonas = (typeof capabilities !== 'undefined' && capabilities && Array.isArray(capabilities.personas))
      ? capabilities.personas
      : [];
    function _personaVisible(personaId) {
      return _isPreviewPersonas || _entitledPersonas.indexOf(personaId) !== -1;
    }
    %>
    <div class="persona-grid">

      <% if (!_isPreviewPersonas && _entitledPersonas.length === 0) { %>
      <!-- Defensive empty state: should not normally be reachable (every
           package config includes at least one persona), but a real user
           seeing a silently empty grid with no explanation would be a
           much worse failure than a clear message pointing at pricing. -->
      <div style="grid-column:1/-1; padding:20px; text-align:center; color:var(--muted); font-size:13.5px;">
        No interview personas are available on your current package. <a href="/#pricing" style="color:var(--blue-hi);">View Plans &amp; Pricing</a>
      </div>
      <% } %>

      <% if (_personaVisible('alex_chen')) { %>
      <div class="sel-card persona-card" data-persona="alex_chen" data-voice="voice-alex" data-diff="Hard (Rigorous)">
        <div class="check">✓</div>
        <div class="p-head"><div class="p-avatar">AC</div><div><div class="p-name">Alex Chen <span class="difficulty diff-hard">🔥 Hard</span></div><div class="p-title">Senior Principal Engineer · AWS</div></div></div>
        <span class="p-style royal-blue">Technical Excellence</span>
        <ul class="p-traits"><li>System Design</li><li>Scalability</li><li>Technical Judgment</li><li>Ownership</li></ul>
      </div>
      <% } %>

      <% if (_personaVisible('priya_ramesh')) { %>
      <div class="sel-card persona-card" data-persona="priya_ramesh" data-voice="voice-priya" data-diff="Elite (Rigorous)">
        <div class="check">✓</div>
        <div class="p-head"><div class="p-avatar emerald">PR</div><div><div class="p-name">Priya Ramesh <span class="difficulty diff-elite">💎 Elite</span></div><div class="p-title">Partner · McKinsey Consulting</div></div></div>
        <span class="p-style emerald">Case-Led Strategy</span>
        <ul class="p-traits"><li>Case Structuring</li><li>Strategic Thinking</li><li>Executive Presence</li><li>Clear Communication</li></ul>
      </div>
      <% } %>

      <% if (_personaVisible('marcus_webb')) { %>
      <div class="sel-card persona-card" data-persona="marcus_webb" data-voice="voice-marcus" data-diff="Medium (Balanced)">
        <div class="check">✓</div>
        <div class="p-head"><div class="p-avatar gold">MW</div><div><div class="p-name">Marcus Webb <span class="difficulty diff-medium">⚡ Medium</span></div><div class="p-title">VP Product · Series B SaaS</div></div></div>
        <span class="p-style gold">Product Leadership</span>
        <ul class="p-traits"><li>Product Strategy</li><li>Prioritization</li><li>Commercial Acumen</li><li>Customer Insight</li></ul>
      </div>
      <% } %>

      <% if (_personaVisible('sanjeev_nair')) { %>
      <div class="sel-card persona-card" data-persona="sanjeev_nair" data-voice="voice-sanjeev" data-diff="Hard (Rigorous)">
        <div class="check">✓</div>
        <div class="p-head"><div class="p-avatar">SN</div><div><div class="p-name">Sanjeev Nair <span class="difficulty diff-hard">🔥 Hard</span></div><div class="p-title">Engineering Director · Global Tech Firm</div></div></div>
        <span class="p-style royal-blue">Engineering Leadership</span>
        <ul class="p-traits"><li>Engineering Strategy</li><li>Organizational Leadership</li><li>Delivery Excellence</li><li>Technical Vision</li></ul>
      </div>
      <% } %>

      <% if (_personaVisible('sarah_kim')) { %>
      <div class="sel-card persona-card" data-persona="sarah_kim" data-voice="voice-sarah" data-diff="Hard (Rigorous)">
        <div class="check">✓</div>
        <div class="p-head"><div class="p-avatar emerald">SK</div><div><div class="p-name">Sarah Kim <span class="difficulty diff-hard">🔥 Hard</span></div><div class="p-title">CEO · Pre-Series A Startup</div></div></div>
        <span class="p-style emerald">Founder Mindset</span>
        <ul class="p-traits"><li>First Principles</li><li>Speed of Execution</li><li>Resilience</li><li>Bias for Action</li></ul>
      </div>
      <% } %>

      <% if (_personaVisible('raj_mehta')) { %>
      <div class="sel-card persona-card" data-persona="raj_mehta" data-voice="voice-raj" data-diff="Elite (Rigorous)">
        <div class="check">✓</div>
        <div class="p-head"><div class="p-avatar gold">RM</div><div><div class="p-name">Raj Mehta <span class="difficulty diff-elite">💎 Elite</span></div><div class="p-title">Global Director · GCC Enterprise Network</div></div></div>
        <span class="p-style gold">Executive Leadership</span>
        <ul class="p-traits"><li>Executive Presence</li><li>Strategic Judgment</li><li>Board Readiness</li><li>Stakeholder Influence</li></ul>
      </div>
      <% } %>

    </div>
  </main>

  <!-- ══════════ RIGHT: LIVE TERMINAL ══════════ -->
  <aside class="terminal">
    <div class="panel">
      <div class="term-header">
        <span class="term-title">Live Terminal</span>
        <span class="live-pill" id="termStatus">Waiting</span>
      </div>

      <div class="term-block">
        <div class="term-label">AI Engine Status</div>
        <div class="engine-row">
          <svg width="44" height="44" viewBox="0 0 44 44" class="engine-ring-svg">
            <defs><linearGradient id="ring-grad" x1="0%" y1="0%" x2="100%" y2="100%"><stop offset="0%" stop-color="#3B82F6"/><stop offset="100%" stop-color="#8B5CF6"/></linearGradient></defs>
            <circle cx="22" cy="22" r="18" fill="none" stroke="url(#ring-grad)" stroke-width="3.5"/>
          </svg>
          <div>
            <div class="engine-title" id="engTitle">Waiting</div>
            <div class="engine-sub">AI systems ready.</div>
            <% if (typeof previewMode !== 'undefined' && previewMode) { %>
              <div class="engine-sub">Explore, then create your free account.</div>
            <% } else { %>
              <div class="engine-sub">Click 'Initialize Session' to begin.</div>
            <% } %>
          </div>
        </div>
      </div>

      <div class="term-block">
        <div class="term-label">STAR Progress (Real-time Detection)</div>
        <div class="star-row">
          <div class="star-step on"><div class="star-dot">S</div><div class="star-name">Situation</div></div>
          <div class="star-line"></div>
          <div class="star-step"><div class="star-dot">T</div><div class="star-name">Task</div></div>
          <div class="star-line"></div>
          <div class="star-step"><div class="star-dot">A</div><div class="star-name">Action</div></div>
          <div class="star-line"></div>
          <div class="star-step"><div class="star-dot">R</div><div class="star-name">Result</div></div>
        </div>
      </div>

      <div class="term-block">
        <div class="term-label">Overall Intelligence Score</div>
        <div class="score-row">
          <div class="score-ring">––</div>
          <div><div class="score-msg">Score will appear here</div><div class="score-sub">after your interview</div></div>
        </div>
      </div>

      <div class="term-block">
        <div class="term-label">5-Vector Intelligence</div>
        <ul class="vec-list">
          <li><span class="vec-ic" style="background:rgba(59,130,246,0.15);color:var(--v-structure);">▣</span>Structure<span class="vec-bar"></span><span class="vec-val">––</span></li>
          <li><span class="vec-ic" style="background:rgba(167,139,250,0.15);color:var(--v-domain);">✦</span>Domain Expertise<span class="vec-bar"></span><span class="vec-val">––</span></li>
          <li><span class="vec-ic" style="background:rgba(34,197,94,0.15);color:var(--v-strategic);">◎</span>Strategic Thinking<span class="vec-bar"></span><span class="vec-val">––</span></li>
          <li><span class="vec-ic" style="background:rgba(251,146,60,0.15);color:var(--v-comm);">💬</span>Communication<span class="vec-bar"></span><span class="vec-val">––</span></li>
          <li><span class="vec-ic" style="background:rgba(245,158,11,0.15);color:var(--v-leadership);">♛</span>Leadership &amp; Execution<span class="vec-bar"></span><span class="vec-val">––</span></li>
        </ul>
      </div>

      <div class="term-block term-block-last">
        <div class="term-label">AI Activity Feed</div>
        <div class="feed-row"><span class="feed-dot"></span><span id="feedMsg"><% if (typeof previewMode !== 'undefined' && previewMode) { %>AI systems ready. Explore the setup, then create your free account.<% } else { %>AI systems ready. Click 'Initialize Session' to begin.<% } %></span></div>
      </div>

      <% if (typeof previewMode !== 'undefined' && previewMode) { %>
        <button class="begin-btn" id="beginBtn">Create Your Free Account →</button>
      <% } else { %>
        <button class="begin-btn" id="beginBtn" disabled>Initialize Session →</button>
      <% } %>
      <div class="begin-sub">🔒 Secure &amp; private</div>
    </div>
  </aside>

</div>

<script>
function switchTab(tab) {
    document.querySelectorAll('.role-tab').forEach(t => t.classList.toggle('active', t.dataset.tab === tab));
    document.querySelectorAll('.role-panel').forEach(p => p.classList.toggle('visible', p.id === 'tab-' + tab));
    // Re-paint Step 1 selections from the single state source so toggling
    // tabs can never surface stale active classes.
    if (window.__miqRenderStep1) window.__miqRenderStep1();
}

(function () {
  // Same state machine and API payload as the previous version of this page —
  // { personaId, roleTitle, experienceLevel, vapiVoiceId } → POST /api/interview/sessions
  var IS_PREVIEW = <%- (typeof previewMode !== 'undefined' && previewMode) ? 'true' : 'false' %>;
  // Drives the entitlement-exhausted modal's upgrade-path logic below —
  // derived server-side from config/product-packages.js's own key order
  // (server.js's /interview route), never hardcoded here.
  var PACKAGE_ORDER = <%- JSON.stringify(typeof packageOrder !== 'undefined' && Array.isArray(packageOrder) ? packageOrder : ['explorer', 'growth', 'leadership']) %>;
  let state = { role: '', exp: null, persona: null, voice: null, org: null, jdText: '' };
  // Bridge for the Tailor-context script below (object shared by reference;
  // its writes to org/jdText are picked up by the POST payload above).
  window.__miqSetupState = state;
  // Bridge sync() too, so the tailor script can re-validate the CTA after
  // Apply Context (declaration below is hoisted — safe to reference here).
  window.__miqSync = function () { sync(); };

  const EXP_LABELS = { fresher: 'Fresher (0–2 yrs)', mid: 'Mid-Career (3–8 yrs)', senior: 'Senior (8–15 yrs)', executive: 'Executive (15+ yrs)' };
  const customInput = document.getElementById('roleCustom');

  document.querySelectorAll('.sel-card').forEach(el => {
    el.addEventListener('click', () => {
      if (el.classList.contains('persona-card')) {
        document.querySelectorAll('.persona-card').forEach(c => c.classList.remove('active'));
        el.classList.add('active');
        state.persona = el.dataset.persona;
        state.voice = el.dataset.voice;
        document.getElementById('ctxPersona').textContent = el.querySelector('.p-name').childNodes[0].textContent.trim();
        document.getElementById('ctxDiff').textContent = el.dataset.diff || '—';
      } else if (el.dataset.exp) {
        el.parentElement.querySelectorAll('.sel-card').forEach(c => c.classList.remove('active'));
        el.classList.add('active');
        state.exp = el.querySelector('.exp-name').textContent.trim();
        document.getElementById('ctxExp').textContent = EXP_LABELS[el.dataset.exp] || state.exp;
      } else {
        // role card (may live in either tab panel) — STRICT SINGLE-CHOICE:
        // selecting a role clears every other role card across BOTH tab
        // grids AND fully resets the "Tailor" context track.
        if (window.__miqTailorReset) window.__miqTailorReset();
        document.querySelectorAll('.role-panel .sel-card').forEach(c => c.classList.remove('active'));
        el.classList.add('active');
        if (el.dataset.custom) {
          customInput.classList.add('show');
          customInput.focus();
          state.role = customInput.value.trim();
        } else {
          customInput.classList.remove('show');
          state.role = el.dataset.role;
        }
        document.getElementById('ctxRole').textContent = state.role || 'Custom (type below)';
      }
      sync();
    });
  });

  // ── Single source of truth for Step 1 visuals. Called on every tab
  //    toggle so active states always re-render from state, never from
  //    stale DOM classes. (Tailor card visuals are owned by its own
  //    script and are reset at mutation time via __miqTailorReset.) ──
  function renderStep1() {
    document.querySelectorAll('.role-panel .sel-card').forEach(c => {
      c.classList.toggle('active', !!state.role && c.dataset.role === state.role);
    });
  }
  window.__miqRenderStep1 = renderStep1;

  customInput.addEventListener('input', () => {
    state.role = customInput.value.trim();
    document.getElementById('ctxRole').textContent = state.role || 'Custom (type below)';
    sync();
  });

  function sync() {
    // Step 1 is satisfied by EITHER a role card OR an applied "Tailor your
    // interview" context (state.tailorApplied set by the tailor script).
    const ready = !!((state.role || state.tailorApplied) && state.persona && state.exp);
    const btn = document.getElementById('beginBtn');
    btn.disabled = IS_PREVIEW ? false : !ready;
    document.getElementById('engTitle').textContent = ready ? 'Ready' : 'Waiting';
    document.getElementById('termStatus').textContent = ready ? 'Ready' : 'Waiting';
    document.getElementById('feedMsg').textContent = IS_PREVIEW
      ? 'AI systems ready. Explore the setup, then create your free account.'
      : (ready
        ? 'All systems ready. Click \u2018Initialize Session\u2019 to launch.'
        : 'AI systems ready. Complete steps 1\u20133 to begin.');
  }

  async function createSessionOnce() {
    const res = await fetch('/api/interview/sessions', {
      method: 'POST',
      headers: {'Content-Type': 'application/json'},
      body: JSON.stringify({
          personaId: state.persona,
          roleTitle: state.role || state.tailorRole || 'General Professional',
          experienceLevel: state.exp,
          vapiVoiceId: state.voice,
          // Tailored-context pipeline: packaged ONLY when the tailor track
          // is the active Step 1 choice (strict single-selection model).
          orgPreset: state.tailorApplied ? (state.org || null) : null,
          jdText: state.tailorApplied ? (state.jdText || '') : ''
      })
    });
    const data = await res.json();
    return { status: res.status, data: data };
  }

  // ── Session Recovery Modal logic (feature, 2026-07-24 follow-up) ──────
  function formatMinutesAgo(lastActiveAt) {
    var mins = Math.max(0, Math.round((Date.now() - new Date(lastActiveAt).getTime()) / 60000));
    if (mins < 1) return 'less than a minute ago';
    if (mins === 1) return '1 minute ago';
    return mins + ' minutes ago';
  }

  function capitalize(s) {
    return s ? s.charAt(0).toUpperCase() + s.slice(1) : s;
  }

  // ── Capability Modal — REUSABLE, not interview-specific ────────────────
  // Any capability failure can call this: showCapabilityModal({ title,
  // body, options, primaryCtaLabel, primaryCtaHref }). This function has
  // zero knowledge of interviews, entitlements, or packages — all of
  // that lives in the adapter functions that call it (showEntitlementModal
  // below is one such adapter; a future "Resume Intelligence unavailable"
  // or "Persona locked" modal would be another, calling this same
  // function with different content, not a copy of this markup/logic).
  //
  // `options` are short, plain description rows — deliberately not a
  // bullet-point feature breakdown and not individually-clickable CTAs.
  // There is exactly ONE actionable button (primaryCtaLabel/Href), since
  // showing several differently-labeled buttons that all currently point
  // at the same non-functional-per-tier destination (no checkout exists
  // yet) would be misleading, not premium.
  function showCapabilityModal(config) {
    document.getElementById('capabilityModalTitle').textContent = config.title || '';
    document.getElementById('capabilityModalBody').textContent = config.body || '';

    var optionsEl = document.getElementById('capabilityModalOptions');
    optionsEl.innerHTML = '';
    (config.options || []).forEach(function (optionText) {
      var row = document.createElement('div');
      row.textContent = optionText;
      row.style.cssText = 'font-size:13px; color:var(--muted); line-height:1.4; padding:8px 0; border-top:1px solid var(--border);';
      optionsEl.appendChild(row);
    });

    var cta = document.getElementById('capabilityModalPrimaryCta');
    cta.textContent = config.primaryCtaLabel || '';
    cta.href = config.primaryCtaHref || '#';

    document.getElementById('capabilityModalOverlay').style.display = 'flex';
  }

  // ── Entitlement-exhausted adapter (INTERVIEW_ENTITLEMENT_EXHAUSTED, 403) ──
  // Translates the structured error + resolved package into a
  // showCapabilityModal() call. This is the ONLY place that knows about
  // interview minutes/packages — the modal component itself doesn't.
  function showEntitlementModal(errorData) {
    var packageId = (errorData.package && errorData.package.id) || 'explorer';
    var entitlement = errorData.entitlement || {};
    var packageLabel = capitalize(packageId);

    // Same phrasing convention as the Settings page's "Interview Minutes:
    // X of Y remaining" line — one short sentence, not a paragraph.
    var body = 'You\'ve used ' + (entitlement.minutesUsed != null ? Math.round(entitlement.minutesUsed) : '?')
      + ' of ' + (entitlement.minutesIncluded != null ? entitlement.minutesIncluded : '?')
      + ' minutes included with your ' + packageLabel + ' package.';

    // Per-package option copy. NOT derived from PACKAGE_ORDER/config —
    // deliberately hardcoded here, since this specific marketing phrasing
    // ("Resume Intelligence, JD Intelligence & 3 AI Interviewers") isn't
    // data that exists anywhere in config/product-packages.js today; that
    // file stores permission KEYS and a minute count, not customer-facing
    // prose. If this needs to change (new persona counts, a 4th package),
    // it's a one-place edit here — small and explicit, not silently
    // auto-generated from config in a way that could produce awkward text.
    var options;
    if (packageId === 'growth') {
      options = ['Top Up Minutes', 'Upgrade to Leadership'];
    } else if (packageId === 'leadership') {
      options = ['Top Up Minutes'];
    } else {
      options = [
        'Growth – 120 interview minutes, Resume Intelligence, JD Intelligence & 3 AI Interviewers.',
        'Leadership – 300 interview minutes, Executive Intelligence & 6 AI Interviewers.',
      ];
    }

    showCapabilityModal({
      title: 'No Interview Minutes Remaining',
      body: body,
      options: options,
      // Single primary CTA, per spec — until real checkout/top-up exists,
      // every path leads to the pricing section (same destination the
      // header's existing "Upgrade to Pro" CTA already uses).
      primaryCtaLabel: 'View Plans & Pricing',
      primaryCtaHref: '/#pricing',
    });
  }

  function showRecoveryModal(session) {
    var overlay = document.getElementById('recoveryModalOverlay');
    var body = document.getElementById('recoveryModalBody');
    var answered = session.answeredCount || 0;
    body.textContent = 'You have an interview in progress'
      + (session.roleTitle ? ' for ' + session.roleTitle : '')
      + ' — question ' + (answered + 1) + ' of 5, last active ' + formatMinutesAgo(session.lastActiveAt) + '. '
      + 'Would you like to continue where you left off, or start a new interview?';
    overlay.style.display = 'flex';

    var resumeBtn = document.getElementById('recoveryResumeBtn');
    var startNewBtn = document.getElementById('recoveryStartNewBtn');
    var btn = document.getElementById('beginBtn');

    function cleanup() {
      overlay.style.display = 'none';
      resumeBtn.onclick = null;
      startNewBtn.onclick = null;
    }

    resumeBtn.onclick = function () {
      // Resume never auto-connects voice or plays audio on its own — the
      // "Continue Interview" gate in interview-session.ejs requires an
      // explicit click before Vapi reconnects, exactly per the founder's
      // requirement. This navigation just gets the candidate back to
      // their existing session; nothing speaks until they choose to.
      cleanup();
      window.location.href = '/interview/session/' + session.id;
    };

    startNewBtn.onclick = async function () {
      cleanup();
      btn.textContent = 'Starting new session...';
      try {
        await fetch('/api/interview/sessions/' + session.id, {
          method: 'DELETE',
          headers: { 'Content-Type': 'application/json' },
          // 'superseded_by_new_session' — its own distinct outcome, per
          // the founder's explicit ask, not a generic cancellation or a
          // timeout. Validated against an allowlist server-side
          // (db/interview.js's abandonSession) regardless of what's sent
          // here — this is just the one real caller of that value today.
          body: JSON.stringify({ reason: 'superseded_by_new_session' }),
        });
      } catch (e) {
        console.warn('[Recovery] could not end previous session before starting new (non-fatal, will still retry creation):', e);
      }
      var retryResult = await createSessionOnce().catch(function (e) { return { status: 0, data: { error: String(e) } }; });
      if (retryResult.data && retryResult.data.sessionId) {
        window.location.href = '/interview/session/' + retryResult.data.sessionId;
        return;
      }
      console.error('[Recovery] Start New failed to create a session:', retryResult);
      alert('Failed to start a new session. Please check console.');
      btn.textContent = 'Initialize Session →';
    };
  }

  var capabilityDismissBtn = document.getElementById('capabilityModalDismiss');
  if (capabilityDismissBtn) {
    capabilityDismissBtn.addEventListener('click', function () {
      document.getElementById('capabilityModalOverlay').style.display = 'none';
    });
  }

  document.getElementById('beginBtn').addEventListener('click', async () => {
    const btn = document.getElementById('beginBtn');
    if (IS_PREVIEW) {
      window.location.href = '/auth/signup';
      return;
    }
    btn.textContent = 'Initializing...';

    // SIMPLIFIED (2026-07-24): stale-session recovery is now entirely
    // server-owned (middleware/guards.js's requireInterviewEntitlement
    // auto-abandons any ACTIVE session that's gone silent for longer than
    // SESSION_INACTIVITY_TIMEOUT_MINUTES before ever returning a
    // conflict) — see db/interview.js's abandonStaleActiveSession. The
    // frontend cleanup loop that used to live here (delete the reported
    // activeSessionId, retry, repeat) is gone: it's no longer this
    // layer's job, and it could only ever clear one stale session per
    // pass anyway, which is exactly what broke when several had
    // accumulated. A 409 ACTIVE_SESSION_EXISTS reaching this code now
    // means a GENUINELY recent session — most likely a second tab still
    // open — which is a real conflict, not something to silently clear.
    let result = await createSessionOnce().catch(function (e) { return { status: 0, data: { error: String(e) } }; });

    if (result.data && result.data.sessionId) {
      window.location.href = '/interview/session/' + result.data.sessionId;
      return;
    }
    if (result.status === 409 && result.data && result.data.reason === 'ACTIVE_SESSION_EXISTS') {
      if (result.data.recoverable && result.data.session) {
        // Tier 2: genuinely recoverable — show the real choice instead of
        // a dead-end block. Deliberately does NOT auto-resume; see the
        // Resume button handler below, and the "Continue Interview" gate
        // in interview-session.ejs for why voice never restarts on its own.
        showRecoveryModal(result.data.session);
        return;
      }
      // Tier 1: still within the live-heartbeat grace period — genuinely
      // active elsewhere, most likely a real second tab. A real conflict.
      console.log('[Launch] genuinely active session exists elsewhere (id=' + result.data.activeSessionId + ') — not stale, real conflict');
      alert('You already have an interview in progress in another tab or window. Please finish or end that session before starting a new one.');
      btn.textContent = 'Initialize Session →';
      return;
    }

    // Structured entitlement error — show the branded modal instead of
    // the generic alert, and instead of wasting 3 pointless retries on a
    // failure that retrying can never fix. Logged to console for
    // debugging (requirement: technical detail in console, friendly
    // explanation in the UI), same as every other branch here.
    if (result.status === 403 && result.data && result.data.reason === 'INTERVIEW_ENTITLEMENT_EXHAUSTED') {
      console.log('[Launch] interview entitlement exhausted:', result.data);
      showEntitlementModal(result.data);
      btn.textContent = 'Initialize Session →';
      return;
    }

    // Persona entitlement bypass attempt (UI shouldn't normally allow
    // this — see the entitlement gating in this page's persona grid —
    // but this endpoint is the real enforcement point, not the UI, so it
    // must be handled here too, not just assumed unreachable). Reuses
    // the same generic showCapabilityModal() as the entitlement-exhausted
    // case above — exactly the kind of second caller this component was
    // built for.
    if (result.status === 403 && result.data && result.data.reason === 'PERSONA_NOT_ENTITLED') {
      console.log('[Launch] persona not entitled on current package:', result.data);
      showCapabilityModal({
        title: 'Persona Not Available',
        body: 'This interviewer isn\'t included in your current package.',
        options: [],
        primaryCtaLabel: 'View Plans & Pricing',
        primaryCtaHref: '/#pricing',
      });
      btn.textContent = 'Initialize Session →';
      return;
    }

    // Any OTHER failure (transient network/backend hiccup) still gets
    // the silent-retry treatment from before.
    const MAX_ATTEMPTS = 3;
    const RETRY_DELAY_MS = 800;
    let lastError = result.data && result.data.error;
    for (let attempt = 2; attempt <= MAX_ATTEMPTS; attempt++) {
      await new Promise(function (r) { setTimeout(r, RETRY_DELAY_MS); });
      try {
        const retryResult = await createSessionOnce();
        if (retryResult.data && retryResult.data.sessionId) {
          window.location.href = '/interview/session/' + retryResult.data.sessionId;
          return;
        }
        console.warn('[Launch] attempt ' + attempt + ' returned no sessionId:', retryResult.data);
        lastError = retryResult.data && retryResult.data.error;
      } catch (e) {
        console.warn('[Launch] attempt ' + attempt + ' threw:', e);
        lastError = e;
      }
    }
    // Only reached if ALL attempts failed.
    console.error('[Launch] all ' + MAX_ATTEMPTS + ' attempts failed:', lastError);
    alert('Failed to launch. Please check console.');
    btn.textContent = 'Initialize Session →';
  });
})();
</script>

<!-- ══════════════════════════════════════════════════════════════════
     TAILOR THIS INTERVIEW — modal + handler. Fully self-contained:
     its own IDs, its own IIFE, zero changes to existing handlers.
     Writes only two new fields onto the shared `state` object
     (state.org, state.jdText) which the session POST above sends.
════════════════════════════════════════════════════════════════════ -->
<div class="tc-overlay" id="tcOverlay" role="dialog" aria-modal="true" aria-labelledby="tcModalTitle">
  <div class="tc-modal">
    <div class="tc-m-head">
      <div class="tc-m-title" id="tcModalTitle">Tailor your interview</div>
      <button class="tc-m-close" type="button" id="tcClose" aria-label="Close">✕</button>
    </div>

    <div class="tc-field-label">Target Company</div>
    <input class="tc-input" id="tcCompanyIn" type="text" maxlength="80"
           placeholder="e.g. Microsoft, Flipkart, a Series B SaaS…" autocomplete="organization">

    <div class="tc-field-label">Target Level <span class="opt">(Optional)</span></div>
    <div class="tc-levels" id="tcLevels">
      <button class="tc-level" type="button" data-level="Senior PM">Senior PM</button>
      <button class="tc-level" type="button" data-level="Principal Engineer">Principal Engineer</button>
      <button class="tc-level" type="button" data-level="L6">L6</button>
      <button class="tc-level" type="button" data-level="Staff">Staff</button>
      <button class="tc-level" type="button" data-level="Director">Director</button>
    </div>

    <div class="tc-field-label">Paste Job Description <span class="opt">(Optional) — supports markdown</span></div>
    <textarea class="tc-textarea" id="tcJdIn" maxlength="5000"
              placeholder="Paste the JD here and MedhaIQ will detect the competencies to probe…"></textarea>
    <div class="tc-count"><span id="tcCount">0</span> / 5000</div>

    <div class="tc-field-label">AI Detected Focus Areas</div>
    <div class="tc-chips" id="tcChips"><span style="font-size:11px;color:var(--muted)">Paste a JD above to detect focus areas…</span></div>

    <div class="tc-field-label">Resume Intelligence</div>
    <div id="tcResumeStatus" style="font-size:12.5px; color:var(--muted); margin-bottom:14px;">Checking…</div>

    <button class="tc-apply" type="button" id="tcApply">✦ Apply Context</button>
    <div class="tc-privacy">🔒 Private &amp; secure — used only to personalize your interview.</div>
  </div>
</div>

<script>
// Resume Intelligence status — READ-ONLY. This fetch never parses or
// modifies anything; it only reports what's already on file (managed at
// /resume). Interview Setup must never trigger parsing itself.
(function () {
  const el = document.getElementById('tcResumeStatus');
  if (!el) return;
  if (window.__MIQ_PREVIEW_DEMO__) {
    el.innerHTML = window.__MIQ_PREVIEW_DEMO__.resumeStatusHtml;
    return;
  }
  fetch('/api/resume/status')
    .then(r => r.json())
    .then((data) => {
      if (data && data.hasResume) {
        el.innerHTML = `✓ Resume on file (${data.competencyCount || 0} competencies detected) — will personalize this interview automatically. <a href="/resume" style="color:var(--blue-hi);">Manage</a>`;
      } else {
        el.innerHTML = `No resume on file yet. <a href="/resume" style="color:var(--blue-hi);">Add one</a> to personalize your questions.`;
      }
    })
    .catch(() => { el.textContent = 'Resume status unavailable.'; });
})();
</script>

<script>
(function () {
  'use strict';
  // Shared setup state — bridged from the main wizard IIFE above. If the
  // bridge is ever missing, bail silently rather than break the page.
  var state = window.__miqSetupState;
  if (!state) { console.warn('[tailor] setup state bridge missing — feature disabled'); return; }
  // ── Element handles. Cards are instanced by class (one per tab) and all
  //    driven in sync; the modal is a single shared element by ID. ─────────
  var cards    = Array.prototype.slice.call(document.querySelectorAll('.tailor-card'));
  var overlay  = document.getElementById('tcOverlay');
  var companyIn= document.getElementById('tcCompanyIn');
  var jdIn     = document.getElementById('tcJdIn');
  var countEl  = document.getElementById('tcCount');
  var chipsEl  = document.getElementById('tcChips');
  var levelsEl = document.getElementById('tcLevels');
  var pickedLevel = '';

  // ── Client-side focus-area detection. Display-only preview: mirrors the
  //    authoritative parser in services/competency-matrix.js on the server. ──
  var TAXONOMY = [
    ['System Design',        /\b(system design|architecture|architect|distributed systems|microservices|scalab)/i],
    ['Cloud Architecture',   /\b(aws|azure|gcp|cloud|kubernetes|docker|devops|ci\/cd|terraform)\b/i],
    ['Scalability',          /\b(scale|scalab|performance|latency|throughput|reliab)/i],
    ['Leadership',           /\b(lead|mentor|coach|direct reports|people manage|grow(ing)? (the )?team|hire|hiring)\b/i],
    ['Mentorship',           /\b(mentor|coach|grow engineers|develop talent)\b/i],
    ['Performance',          /\b(performance|optimi[sz]|efficien)/i],
    ['Reliability',          /\b(reliab|uptime|sla|incident|on[- ]call|resilien)/i],
    ['Security',             /\b(security|compliance|gdpr|soc ?2|privacy)\b/i],
    ['Stakeholder Comms',    /\b(stakeholder|cross[- ]functional|alignment|executive|influenc)/i],
    ['Strategic Thinking',   /\b(strateg|roadmap|vision|prioriti[sz]|okr)/i],
    ['Data-Driven',          /\b(data[- ]driven|metrics|kpi|analytics|experiment)/i],
    ['Machine Learning',     /\b(machine learning|deep learning|llm|ai model|nlp|mlops)\b/i],
    ['Product Sense',        /\b(product (sense|thinking)|user (experience|research)|customer)\b/i],
    ['Delivery Execution',   /\b(deliver|execution|ship|deadline|agile|scrum|sprint)\b/i]
  ];

  function detectAreas(text) {
    var out = [];
    if (!text) return out;
    for (var i = 0; i < TAXONOMY.length && out.length < 8; i++) {
      if (TAXONOMY[i][1].test(text)) out.push(TAXONOMY[i][0]);
    }
    return out;
  }

  // Mirrors the server merge (5 role defaults + 2 org traits + JD detected,
  // deduped, capped at 8) so the count shown matches what the engine builds.
  function matrixCount(detected, hasCompany) {
    var base = 5 + (hasCompany ? 2 : 2); // role defaults + trait floor
    return Math.min(8, base + detected.length);
  }

  function renderChips() {
    var areas = detectAreas(jdIn.value);
    chipsEl.innerHTML = '';
    if (!areas.length) {
      chipsEl.innerHTML = '<span style="font-size:11px;color:var(--muted)">Paste a JD above to detect focus areas…</span>';
      return;
    }
    areas.forEach(function (a) {
      var s = document.createElement('span');
      s.className = 'tc-chip';
      s.textContent = a;
      chipsEl.appendChild(s);
    });
  }

  // ── Modal open/close ─────────────────────────────────────────────────────
  function openModal() {
    overlay.classList.add('open');
    companyIn.value = state.org || '';
    jdIn.value = state.jdText || '';
    countEl.textContent = jdIn.value.length;
    renderChips();
    setTimeout(function(){ companyIn.focus(); }, 50);
  }
  function closeModal() { overlay.classList.remove('open'); }

  cards.forEach(function (card) {
    card.addEventListener('click', function (e) {
      // The Edit link has its own handler — everything else opens the modal.
      if (e.target.classList && e.target.classList.contains('tc-edit-link')) return;
      openModal();
    });
    card.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); openModal(); }
    });
    var edit = card.querySelector('.tc-edit-link');
    if (edit) edit.addEventListener('click', function (e) { e.stopPropagation(); openModal(); });
  });
  document.getElementById('tcClose').addEventListener('click', closeModal);
  overlay.addEventListener('click', function (e) { if (e.target === overlay) closeModal(); });
  document.addEventListener('keydown', function (e) { if (e.key === 'Escape' && overlay.classList.contains('open')) closeModal(); });

  // ── Reset bridge: called by the role-card handler when the user chooses
  //    the standard track. Fully clears the tailor track — state hooks,
  //    both card instances' visuals, modal chip selection, sidebar row —
  //    so exactly ONE Step 1 selection ever exists. ──
  window.__miqTailorReset = function () {
    if (!state.tailorApplied && !state.org && !state.jdText && !pickedLevel) return;
    state.org = null;
    state.jdText = '';
    state.tailorApplied = false;
    state.tailorRole = '';
    pickedLevel = '';
    levelsEl.querySelectorAll('.tc-level').forEach(function (b) { b.classList.remove('active'); });
    cards.forEach(function (card) {
      card.classList.remove('tc-success');
      var e = card.querySelector('.tc-state-empty');
      var d = card.querySelector('.tc-state-done');
      if (e) e.style.display = '';
      if (d) d.style.display = 'none';
    });
    var wrap = document.getElementById('ctxCompanyWrap');
    if (wrap) wrap.style.display = 'none';
  };

  // ── Level chips (single-select toggle) ───────────────────────────────────
  levelsEl.addEventListener('click', function (e) {
    var btn = e.target.closest('.tc-level');
    if (!btn) return;
    var was = btn.classList.contains('active');
    levelsEl.querySelectorAll('.tc-level').forEach(function (b) { b.classList.remove('active'); });
    if (!was) btn.classList.add('active');
    pickedLevel = was ? '' : btn.dataset.level;
  });

  // ── Live JD character count + detection ─────────────────────────────────
  jdIn.addEventListener('input', function () {
    countEl.textContent = jdIn.value.length;
    renderChips();
  });

  // ── Apply Context: save into the shared state, flip the card, sync sidebar
  document.getElementById('tcApply').addEventListener('click', function () {
    var company = companyIn.value.trim();
    var jd = jdIn.value.trim();
    if (!company && !jd) { companyIn.focus(); return; }

    // The ONLY writes into shared state — read by the session POST payload.
    state.org = company || null;
    state.jdText = jd || '';
    // Validation gateway: an applied context is a first-class Step 1 choice.
    state.tailorApplied = true;
    state.tailorRole = pickedLevel || '';
    // STRICT SINGLE-CHOICE: choosing the tailor track clears any standard
    // role selection across both tab grids (state + visuals + sidebar).
    state.role = '';
    document.querySelectorAll('.role-panel .sel-card').forEach(function (c) { c.classList.remove('active'); });
    var customIn = document.getElementById('roleCustom');
    if (customIn) { customIn.classList.remove('show'); customIn.value = ''; }

    // ── Company logo resolution (favicon service, SVG building fallback) ──
    var KNOWN_DOMAINS = {
      google:'google.com', microsoft:'microsoft.com', amazon:'amazon.com', apple:'apple.com',
      meta:'meta.com', facebook:'meta.com', netflix:'netflix.com', nvidia:'nvidia.com',
      adobe:'adobe.com', salesforce:'salesforce.com', oracle:'oracle.com', ibm:'ibm.com',
      intel:'intel.com', walmart:'walmart.com', accenture:'accenture.com', deloitte:'deloitte.com',
      infosys:'infosys.com', tcs:'tcs.com', wipro:'wipro.com', flipkart:'flipkart.com',
      uber:'uber.com', airbnb:'airbnb.com', stripe:'stripe.com', 'jpmorgan':'jpmorganchase.com',
      'goldman sachs':'goldmansachs.com', 'mckinsey':'mckinsey.com', linkedin:'linkedin.com'
    };
    function companyDomain(name) {
      var key = name.toLowerCase().trim();
      if (KNOWN_DOMAINS[key]) return KNOWN_DOMAINS[key];
      var slug = key.replace(/[^a-z0-9]/g, '');
      return slug ? slug + '.com' : '';
    }
    var logoUrl = company ? 'https://www.google.com/s2/favicons?domain=' + encodeURIComponent(companyDomain(company)) + '&sz=64' : '';

    var detected = detectAreas(jd);
    var total = matrixCount(detected, !!company);

    // Flip EVERY card instance (both tabs) to the success state in sync
    cards.forEach(function (card) {
      // Logo: try the favicon; on failure (or no company) show the SVG fallback
      var img = card.querySelector('.tc-logo-img');
      var fb  = card.querySelector('.tc-logo-fallback');
      if (img && fb) {
        if (logoUrl) {
          img.onload  = function () { img.style.display = 'block'; fb.style.display = 'none'; };
          img.onerror = function () { img.style.display = 'none';  fb.style.display = 'block'; };
          img.src = logoUrl;
        } else {
          img.style.display = 'none'; fb.style.display = 'block';
        }
      }
      card.querySelector('.tc-company-out').textContent = company || 'Tailored Interview';
      card.querySelector('.tc-role-out').textContent =
        pickedLevel ? pickedLevel + (state.role ? ' · ' + state.role : '') : (state.role || 'Role: pick in Step 1');
      var jdBadge = card.querySelector('.tc-jd-badge');
      jdBadge.style.display = jd ? 'inline-block' : 'none';
      var lvlBadge = card.querySelector('.tc-level-badge');
      lvlBadge.style.display = pickedLevel ? 'inline-block' : 'none';
      lvlBadge.textContent = pickedLevel;
      card.querySelector('.tc-metrics-out').textContent = total + ' AI competencies detected';
      var stEmpty = card.querySelector('.tc-state-empty');
      var stDone  = card.querySelector('.tc-state-done');
      stEmpty.style.display = 'none';
      stDone.style.display = 'flex';
      stDone.style.flexDirection = 'column';
      stDone.style.gap = '8px';
      stDone.style.height = '100%';
      card.classList.add('tc-success');
    });

    // Sidebar live update (Company Name · JD Status · Competency Count)
    document.getElementById('ctxCompanyWrap').style.display = '';
    document.getElementById('ctxCompany').textContent = company || 'Tailored';
    document.getElementById('ctxCompanyMeta').textContent =
      (jd ? 'JD attached · ' : 'No JD · ') + total + ' competencies';

    // Reflect the tailored track in the sidebar Role row (the standard role
    // selection was just cleared — one track, one truth).
    var ctxRole = document.getElementById('ctxRole');
    if (ctxRole) ctxRole.textContent = pickedLevel ? pickedLevel + ' (tailored)' : 'Tailored to JD';

    // Re-run the master form validation so Initialize Session unlocks.
    if (window.__miqSync) window.__miqSync();

    closeModal();
  });
})();
</script><!-- ── Session Recovery Modal (feature, 2026-07-24 follow-up) ──────────────
     Shown only when the server reports a RECOVERABLE conflict (tier 2 of
     the three-tier design in middleware/guards.js): a confirmed session
     that's gone quiet longer than the live-heartbeat grace period but is
     still within the recovery window. Deliberately NOT a native alert/
     confirm() — a real choice (Resume / Start New) for what the founder
     specifically wants to be treated as a premium-product moment, not a
     dead-end block. -->
<div id="recoveryModalOverlay" style="display:none; position:fixed; inset:0; background:rgba(2,6,16,0.75); z-index:9999; align-items:center; justify-content:center;">
  <div style="background:var(--card); border:1px solid var(--border); border-radius:16px; max-width:440px; width:90%; padding:28px; box-shadow:0 20px 60px rgba(0,0,0,0.5);">
    <div style="font-size:18px; font-weight:700; color:var(--white); margin-bottom:10px;">Continue your interview?</div>
    <div id="recoveryModalBody" style="font-size:14px; color:var(--muted); line-height:1.6; margin-bottom:22px;"></div>
    <div style="display:flex; gap:10px;">
      <button id="recoveryResumeBtn" style="flex:1; padding:12px; border-radius:10px; border:none; background:var(--blue-hi); color:#fff; font-weight:600; font-size:14px; cursor:pointer;">Resume Interview</button>
      <button id="recoveryStartNewBtn" style="flex:1; padding:12px; border-radius:10px; border:1px solid var(--border); background:transparent; color:var(--muted); font-weight:600; font-size:14px; cursor:pointer;">Start New</button>
    </div>
  </div>
</div>

<!-- Capability Modal — a REUSABLE component, not interview-specific.
     Any capability failure (entitlement exhausted, Resume Intelligence
     locked, a locked Persona, JD Intelligence unavailable, report
     generation unavailable, etc.) can show this same modal via
     showCapabilityModal({ title, body, options, primaryCtaLabel,
     primaryCtaHref }) — see the function definition in the script below.
     This markup block has zero interview-specific knowledge; all of
     that lives in the small adapter function (showEntitlementModal)
     that calls this. Compact by design — one short body line, a few
     plain option rows (no bullet lists), and exactly one primary CTA,
     not an attempt to reproduce the pricing page in a modal. -->
<div id="capabilityModalOverlay" style="display:none; position:fixed; inset:0; background:rgba(2,6,16,0.75); z-index:9999; align-items:center; justify-content:center;">
  <div style="background:var(--card); border:1px solid var(--border); border-radius:16px; max-width:400px; width:90%; padding:24px; box-shadow:0 20px 60px rgba(0,0,0,0.5);">
    <div id="capabilityModalTitle" style="font-size:17px; font-weight:700; color:var(--white); margin-bottom:8px;"></div>
    <div id="capabilityModalBody" style="font-size:13.5px; color:var(--muted); line-height:1.5; margin-bottom:16px;"></div>
    <div id="capabilityModalOptions" style="margin-bottom:16px;"></div>
    <a id="capabilityModalPrimaryCta" href="#" style="display:block; text-align:center; padding:11px; border-radius:10px; font-weight:600; font-size:14px; text-decoration:none; background:var(--bright); color:#fff;"></a>
    <button id="capabilityModalDismiss" style="width:100%; margin-top:8px; padding:8px; border-radius:10px; border:none; background:transparent; color:var(--muted); font-weight:600; font-size:12.5px; cursor:pointer;">Not now</button>
  </div>
</div>

</body>
</html>