<!DOCTYPE html>
<html lang="en">
<head><meta name="google-site-verification" content="891zCX6HOhXmVEqnjJKxkg5uP6W46PVuWtuOpNPl9a8" />
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MedhaIQ | AI Career Intelligence Platform </title>
 <meta
  name="description"
  content="MedhaIQ is an AI Career Intelligence Platform that helps professionals practice interviews, measure career readiness, and accelerate long-term career growth.">
  <meta name="robots" content="index, follow">
  <link rel="canonical" href="https://www.medhaiq.ai/">
 <!-- Favicon — sourced from the same brand-mark asset used by the live
       header logo (public/assets/branding/medhaiq-icon.png / brand-logo.ejs),
       padded onto a square transparent canvas for favicon format only. -->
  <link rel="icon" type="image/png" href="/assets/branding/favicon.png">
  <link rel="apple-touch-icon" href="/assets/branding/favicon.png">
  <meta name="theme-color" content="#0A0F1E">
  <!-- OG / Social -->
  <meta property="og:title" content="MedhaIQ | AI Career Intelligence Platform">
  <meta property="og:description" content="Practice interviews, measure career readiness, and accelerate your professional growth with MedhaIQ.">
  <meta property="og:type" content="website">
  <meta property="og:image" content="https://pub-629428d185ca4960a0a73c850d32294b.r2.dev/generated-images/company_214395/og-medhaiq.jpg">
  <!-- Web Manifest — static file so its icon path resolves relative to
       whichever domain serves it (staging or production). -->
  <link rel="manifest" href="/manifest.json">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/medhaiq.css">
  <link rel="stylesheet" href="/css/components.css">
  <link rel="stylesheet" href="/css/hero-v2.css">
  <link rel="stylesheet" href="/css/value-footer.css">
  <link rel="stylesheet" href="/css/career-preview.css">
  <link rel="stylesheet" href="/css/homepage.css">
  <!-- Structured data — Organization + WebSite. Facts limited to what's
       already published elsewhere on the site (name, canonical URL,
       brand logo asset): no invented founder/address/social/schema. -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Organization",
    "name": "MedhaIQ",
    "url": "https://www.medhaiq.ai/",
    "logo": "https://www.medhaiq.ai/assets/branding/medhaiq-icon.png"
  }
  </script>
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "MedhaIQ",
    "url": "https://www.medhaiq.ai/"
  }
  </script>
</head>
<body>
  <%- include('partials/header') %>
  <%- include('partials/hero') %>
  <%- include('partials/value-footer') %>
  <%- include('partials/trust-bar') %>
  <%- include('partials/features') %>
  <%- include('partials/score-demo') %>
  <%- include('partials/recruiter') %>
  <%# Certifications and Research sections hidden from the home page
      (2026-07-25, explicit request) - NOT deleted. Partial files at
      views/partials/certifications.ejs and views/partials/research.ejs
      are untouched. To re-enable on this page, or move them to a
      different page later, add back an include tag for
      partials slash certifications and partials slash research,
      right where this comment is. %>
  <%- include('partials/pricing-section') %>
  <%- include('partials/cta-banner') %>
  <%- include('partials/footer') %>

  <!-- Fix: on a fresh page load with a URL hash (e.g. /#professional-horizons),
       the browser scrolls to the anchor before the async Google Fonts finish
       loading. When the font swaps in, every section above the anchor
       reflows slightly, and those small shifts accumulate this far down the
       page — the scroll position ends up wrong (usually back near the top)
       even though the browser did the right thing initially. Re-applying
       the scroll once fonts are confirmed ready corrects it. Purely
       corrective; does nothing on pages/loads without a hash. -->
  <script>
  (function() {
    if (!window.location.hash) return;
    if (!document.fonts || !document.fonts.ready) return;
    document.fonts.ready.then(function() {
      var target = document.querySelector(window.location.hash);
      if (target) target.scrollIntoView({ behavior: 'instant', block: 'start' });
    });
  })();
  </script>

  <!-- Scroll reveal: IntersectionObserver for .reveal elements -->
  <script>
  (function() {
    var reveals = document.querySelectorAll('.reveal');
    if (!reveals.length) return;
    var observer = new IntersectionObserver(function(entries) {
      entries.forEach(function(entry) {
        if (entry.isIntersecting) {
          entry.target.classList.add('visible');
          var items = entry.target.querySelectorAll('.reveal-item');
          items.forEach(function(item, i) {
            setTimeout(function() { item.classList.add('visible'); }, i * 100);
          });
        }
      });
    }, { threshold: 0.08, rootMargin: '0px 0px -40px 0px' });
    reveals.forEach(function(el) { observer.observe(el); });
  })();
  </script>

  <!-- Global modal trigger — opens the email capture modal from any page -->
  <script>
  (function() {
    function openModal(plan) {
      var modal = document.getElementById('mh-email-modal');
      if (!modal) return;
      modal.style.display = 'flex';
      var planField = document.getElementById('mh-modal-plan');
      if (planField) planField.value = plan || 'free';
      var form = document.getElementById('mh-modal-form');
      if (form) form.style.display = 'flex';
      var success = document.getElementById('mh-modal-success');
      if (success) success.style.display = 'none';
    }
    // Wire all .mh-open-modal buttons (hero, nav CTAs)
    document.querySelectorAll('.mh-open-modal').forEach(function(btn) {
      btn.addEventListener('click', function() {
        openModal(btn.getAttribute('data-plan') || 'free');
      });
    });
    // Close handlers
    var closeBtn = document.getElementById('mh-modal-close');
    var modal = document.getElementById('mh-email-modal');
    if (closeBtn) closeBtn.addEventListener('click', function() {
      if (modal) modal.style.display = 'none';
    });
    if (modal) {
      modal.addEventListener('click', function(e) {
        if (e.target === modal) modal.style.display = 'none';
      });
    }
    document.addEventListener('keydown', function(e) {
      if (e.key === 'Escape' && modal && modal.style.display === 'flex') {
        modal.style.display = 'none';
      }
    });
  })();
  </script>

  <!-- Polsia Analytics Beacon -->
  <script>
    (function() {
      var slug = 'medhaiq';
      var vid = localStorage.getItem('polsia_vid');
      if (!vid) {
        vid = 'xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx'.replace(/[xy]/g, function(c) {
          var r = Math.random() * 16 | 0;
          return (c === 'x' ? r : (r & 0x3 | 0x8)).toString(16);
        });
        localStorage.setItem('polsia_vid', vid);
      }
      new Image().src = 'https://polsia.com/api/beacon/pixel?s=' + encodeURIComponent(slug) + '&v=' + encodeURIComponent(vid);
    })();
  </script>

  <%- include('partials/contact-modal') %>
</body>
</html>