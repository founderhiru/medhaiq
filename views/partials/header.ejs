<header class="mh-nav" id="mh-nav">
  <div class="mh-nav-inner">
    <!-- Logo — unchanged -->
    <a href="/" class="mh-logo" aria-label="MedhaIQ.ai home">
      <div class="mh-logo-mark">
        <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg">
          <path d="M3 3 L10 3 L10 10 L17 10" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          <circle cx="10" cy="10" r="1.5" fill="white"/>
          <circle cx="3" cy="3" r="1.5" fill="white"/>
          <circle cx="17" cy="10" r="1.5" fill="white"/>
          <path d="M10 10 L17 3" stroke="white" stroke-width="1.5" stroke-linecap="round"/>
          <path d="M7 14 L7 17 L10 17" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
      </div>
      <span class="mh-logo-text">Medha<span>IQ</span>.ai</span>
    </a>

    <!-- Desktop nav -->
    <nav class="mh-nav-links" aria-label="Main navigation">

      <!-- "Platform" is now a mega-menu trigger, not a direct link.
           Previously <a href="#platform">, which pointed to the Advanced
           Platform Architecture section (architecture.ejs). That section
           is unchanged and still reachable by scrolling — it's just no
           longer the nav word's direct destination. -->
      <div class="mh-nav-item mh-nav-item-mega" id="mh-platform-wrap">
        <button type="button"
                class="mh-nav-link mh-nav-trigger"
                id="mh-platform-trigger"
                aria-haspopup="true"
                aria-expanded="false">
          Platform
          <svg class="mh-nav-chevron" width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
        </button>

        <div class="mh-megamenu" id="mh-megamenu" role="menu" aria-label="Platform capabilities">
          <div class="mh-megamenu-inner">

            <!-- Column A: Product Capabilities -->
            <div class="mh-megamenu-col">
              <div class="mh-megamenu-col-label">Product Capabilities</div>
              <a href="/interview" class="mh-megamenu-item" role="menuitem">
                <span class="mh-megamenu-icon">🎤</span>
                <span class="mh-megamenu-text">
                  <span class="mh-megamenu-title">Adaptive AI Interview</span>
                  <span class="mh-megamenu-desc">Personalized interview sessions</span>
                </span>
              </a>
              <a href="/interview" class="mh-megamenu-item" role="menuitem">
                <span class="mh-megamenu-icon">⭐</span>
                <span class="mh-megamenu-text">
                  <span class="mh-megamenu-title">Interview Insights</span>
                  <span class="mh-megamenu-desc">Executive summaries, STAR analysis &amp; coaching</span>
                </span>
              </a>
              <a href="/dashboard/history" class="mh-megamenu-item" role="menuitem">
                <span class="mh-megamenu-icon">📈</span>
                <span class="mh-megamenu-text">
                  <span class="mh-megamenu-title">Career Progress</span>
                  <span class="mh-megamenu-desc">History, trends and improvement tracking</span>
                </span>
              </a>
            </div>

            <!-- Column B: Solutions — labels match audience.ejs's real card titles,
                 not the prompt's "Engineering/Leadership/Career Transition" (those
                 don't exist in the repo — see conflict note in chat). -->
            <div class="mh-megamenu-col">
              <div class="mh-megamenu-col-label">Solutions</div>
              <a href="/#professional-horizons" class="mh-megamenu-item" role="menuitem">
                <span class="mh-megamenu-icon">🎓</span>
                <span class="mh-megamenu-text">
                  <span class="mh-megamenu-title">Students &amp; Campus Graduates</span>
                  <span class="mh-megamenu-desc">Campus-ready interview prep</span>
                </span>
              </a>
              <a href="/#professional-horizons" class="mh-megamenu-item" role="menuitem">
                <span class="mh-megamenu-icon">📊</span>
                <span class="mh-megamenu-text">
                  <span class="mh-megamenu-title">Early &amp; Mid-Career Professionals</span>
                  <span class="mh-megamenu-desc">Career accelerator track</span>
                </span>
              </a>
              <a href="/#professional-horizons" class="mh-megamenu-item" role="menuitem">
                <span class="mh-megamenu-icon">👔</span>
                <span class="mh-megamenu-text">
                  <span class="mh-megamenu-title">Senior Managers &amp; Directors</span>
                  <span class="mh-megamenu-desc">Leadership track</span>
                </span>
              </a>
            </div>

          </div>
        </div>
      </div>

      <!-- "Why MedhaIQ" — NO /why route exists in the repo. Standing in with
           #matrix (matrix.ejs, "A Paradigm Shift in Career Engineering") until
           you confirm a real destination — see conflict note in chat. -->
      <a href="/#matrix" class="mh-nav-link">Why MedhaIQ</a>
      <a href="#pricing" class="mh-nav-link">Pricing</a>
    </nav>

    <!-- CTA + hamburger — unchanged -->
    <div class="mh-nav-actions">
      <a href="/login" class="mh-nav-cta" style="margin-right: 8px;">Start Free Trial</a>
      <button class="mh-hamburger" id="mh-hamburger" aria-label="Toggle menu" aria-expanded="false">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </div>

  <!-- Mobile menu -->
  <div class="mh-mobile-menu" id="mh-mobile-menu">
    <nav class="mh-mobile-nav">

      <!-- Platform becomes an accordion item on mobile, not a menu link.
           Two columns collapse into two labeled groups, same content/order. -->
      <div class="mh-mobile-accordion">
        <button type="button" class="mh-mobile-link mh-mobile-accordion-trigger" id="mh-mobile-platform-trigger" aria-expanded="false">
          Platform
          <svg class="mh-mobile-chevron" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="6 9 12 15 18 9"/></svg>
        </button>
        <div class="mh-mobile-accordion-panel" id="mh-mobile-platform-panel">
          <div class="mh-mobile-group-label">Product Capabilities</div>
          <a href="/interview" class="mh-mobile-sublink">🎤 Adaptive AI Interview</a>
          <a href="/interview" class="mh-mobile-sublink">⭐ Interview Insights</a>
          <a href="/dashboard/history" class="mh-mobile-sublink">📈 Career Progress</a>
          <div class="mh-mobile-group-label">Solutions</div>
          <a href="/#professional-horizons" class="mh-mobile-sublink">🎓 Students &amp; Campus Graduates</a>
          <a href="/#professional-horizons" class="mh-mobile-sublink">📊 Early &amp; Mid-Career Professionals</a>
          <a href="/#professional-horizons" class="mh-mobile-sublink">👔 Senior Managers &amp; Directors</a>
        </div>
      </div>

      <a href="/#matrix" class="mh-mobile-link">Why MedhaIQ</a>
      <a href="#pricing" class="mh-mobile-link">Pricing</a>
      <a href="/login" class="mh-mobile-cta">Start Free Trial</a>
    </nav>
  </div>
</header>

<script>
(function() {
  var nav = document.getElementById('mh-nav');
  var hamburger = document.getElementById('mh-hamburger');
  var mobileMenu = document.getElementById('mh-mobile-menu');

  /* ---- existing scroll behavior — unchanged ---- */
  window.addEventListener('scroll', function() {
    if (window.scrollY > 60) {
      nav.classList.add('mh-nav-scrolled');
    } else {
      nav.classList.remove('mh-nav-scrolled');
    }
  });

  /* ---- existing hamburger toggle — unchanged ---- */
  hamburger.addEventListener('click', function() {
    var isOpen = mobileMenu.classList.toggle('mh-mobile-menu-open');
    hamburger.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
    hamburger.classList.toggle('mh-hamburger-open', isOpen);
  });

  /* ---- existing "close mobile menu on link click" — unchanged.
     Note: the mobile Platform trigger is a <button>, not an <a>, so this
     querySelectorAll('a') correctly skips it — tapping it opens the
     accordion instead of closing the whole mobile menu. ---- */
  mobileMenu.querySelectorAll('a').forEach(function(link) {
    link.addEventListener('click', function() {
      mobileMenu.classList.remove('mh-mobile-menu-open');
      hamburger.classList.remove('mh-hamburger-open');
      hamburger.setAttribute('aria-expanded', 'false');
    });
  });

  /* ---- NEW: desktop mega menu (hover + focus + click + Escape) ---- */
  var megaWrap = document.getElementById('mh-platform-wrap');
  var megaTrigger = document.getElementById('mh-platform-trigger');

  function openMega() {
    megaWrap.classList.add('mh-mega-open');
    megaTrigger.setAttribute('aria-expanded', 'true');
  }
  function closeMega() {
    megaWrap.classList.remove('mh-mega-open');
    megaTrigger.setAttribute('aria-expanded', 'false');
  }

  megaWrap.addEventListener('mouseenter', openMega);
  megaWrap.addEventListener('mouseleave', closeMega);

  // Click/tap toggles — covers touch devices where hover doesn't fire
  megaTrigger.addEventListener('click', function(e) {
    e.preventDefault();
    e.stopPropagation(); // prevent the document click-outside listener below from immediately re-closing what this click just opened
    if (megaWrap.classList.contains('mh-mega-open')) { closeMega(); }
    else { openMega(); }
  });

  // Keyboard: Tab into the menu opens it, Tab out closes it
  megaWrap.addEventListener('focusin', openMega);
  megaWrap.addEventListener('focusout', function(e) {
    if (!megaWrap.contains(e.relatedTarget)) { closeMega(); }
  });

  // Escape closes and returns focus to the trigger — required by spec
  document.addEventListener('keydown', function(e) {
    if (e.key === 'Escape' && megaWrap.classList.contains('mh-mega-open')) {
      closeMega();
      megaTrigger.focus();
    }
  });

  // NEW: click anywhere outside the menu closes it — this was missing
  // entirely before, which is part of why "click" felt broken (nothing
  // closed it predictably, so state got confusing after a stray click).
  document.addEventListener('click', function(e) {
    if (megaWrap.classList.contains('mh-mega-open') && !megaWrap.contains(e.target)) {
      closeMega();
    }
  });

  /* ---- NEW: mobile Platform accordion ---- */
  var mobTrigger = document.getElementById('mh-mobile-platform-trigger');
  var mobPanel = document.getElementById('mh-mobile-platform-panel');

  mobTrigger.addEventListener('click', function() {
    var isOpen = mobPanel.classList.toggle('mh-mobile-accordion-panel-open');
    mobTrigger.setAttribute('aria-expanded', isOpen ? 'true' : 'false');
  });
})();
</script>
